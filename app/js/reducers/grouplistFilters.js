const initialState = {};

const grouplistFilters = (state = initialState, action) => {
	switch (action.type) {
		// case 'CHANGE_FILTER':
				
		// 	let newState = state;
			
		// 	newState[action.filterColumn] = {
		// 		type: action.filterType,
		// 		value: action.filterValue
		// 	}

		// 	return newState;
		// 	break;

		default:
			return state
	}
}

export default grouplistFilters;